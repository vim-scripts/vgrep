Add following vim/script.vim for vim7 with grp syntax color
    " leGRP
  elseif s:line1 =~ '^File:'
    set ft=grp


install 
	vgrep on /usr/local/bin
	grp.vim ~/.vim/syntax
	vgrep.vim ~/.vim/plugin

